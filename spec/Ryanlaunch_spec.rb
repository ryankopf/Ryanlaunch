# frozen_string_literal: true

RSpec.describe Ryanlaunch do
  it "has a version number" do
    expect(Ryanlaunch::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
